schedule:
  time: "08:30"
  timezone: "America/Sao_Paulo"

keywords: [] # Depreciado: use 'rules' abaixo para maior controle

rules:
  # Exemplo 1: Regra que busca termos no corpo de QUALQUER documento (default seções)
  - name: "Nomeações na Força Nacional"
    title_terms:
      - "PORTARIA MJSP"
    body_terms:
      - "força nacional"
    sections: ["dou1"]



storage:
  output_dir: "data"
  format: "jsonl"

logging:
  level: "INFO"
  file: "logs/scrapper.log"
